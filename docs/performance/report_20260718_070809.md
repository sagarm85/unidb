# Multi-model at-scale report

_Generated by `scripts/multi_model_report.sh` — self-contained; no other docs needed._

| | |
|---|---|
| Date | 2026-07-18 07:08:09 UTC |
| Commit | `cd94d71` (branch `main`) |
| Machine | aarch64 · 18 cores · Linux 6.12.76-linuxkit |
| Build | `--release`, group-commit (one `fsync` per commit) |
| Sizes swept | 1000,10000,100000 |
| Marginal sample | 200 commits/point |
| Postgres | set (Postgres column measured) |
| Realistic stack (Table 4.1) | unset (Table 4.1 skipped) |
| Peak RSS | — (bench killed before completion; see note) |
| Time taken | partial — Tables 1–4 (1k/10k/100k) extracted; 1M W4 phase killed (~6h ETA) |

> **Extraction note:** The bench binary was killed during `t4_unidb_1000000`
> (1M-row W4 throughput, running 80+ minutes at ~42 txns/s → ~6h remaining).
> All data below was recovered from the bench's live stdout buffer
> (`/proc/47/fd/1`). Tables 1–4 at 1k/10k/100k are complete and correct.
> `MM_TX_SWEEP` will be capped at 100k in the next run to prevent recurrence.

---

## What this measures (self-contained — no other docs needed)

The **W0→W4 decomposition ladder**: one durable transaction, adding one data
model at a time, so the multi-model write tax is read by subtraction. Every
commit is one group-coalesced `fsync` (matched durability). The number
reported is **marginal ms per durable commit** at each pre-grown table size
(`MM_SAMPLE=200` commits averaged; pre-grow not timed).

| Rung | What the single transaction does |
|------|----------------------------------|
| **W0** | plain relational row INSERT (WAL + heap + fsync) |
| **W1** | W0 + a B-tree secondary index entry |
| **W2** | W1 + a `VECTOR(128)` value + its ANN index (HNSW) |
| **W3** | W2 + a graph edge + adjacency-index maintenance |
| **W4** | W3 + event-queue capture — **the full four-model commit** |

`W4−W0` is the total multi-model tax; `W4/W0` is the multiplier. The thesis:
one shared fsync makes W4 ≈ W0 (not N×), and it should stay that way as rows
grow. A rising `W4/W0` means per-model index CPU (esp. HNSW) is eroding it.

## Table 1 — Multi-model commit cost vs table size (ms/commit)

| rows | W0 | W1 | W2 | W3 | W4 | W4−W0 | W4/W0 |
|-----:|---:|---:|---:|---:|---:|------:|------:|
| 1000 | 1.06 | 0.94 | 7.43 | 8.02 | 10.78 | 9.72 | 10.16× |
| 10000 | 0.45 | 0.53 | 6.07 | 6.90 | 7.03 | 6.59 | 15.80× |
| 100000 | 0.21 | 0.22 | 19.09 | 26.39 | 37.97 | 37.76 | 181.53× |

## Table 2 — Per-model marginal maintenance vs size (ms added per commit)

| rows | Δ btree (W1−W0) | Δ vector (W2−W1) | Δ edge (W3−W2) | Δ event (W4−W3) |
|-----:|----------------:|-----------------:|---------------:|----------------:|
| 1000 | -0.12 | +6.49 | +0.58 | +2.76 |
| 10000 | +0.09 | +5.54 | +0.83 | +0.14 |
| 100000 | +0.02 | +18.87 | +7.30 | +11.58 |

*(small-size deltas are near-noise — every rung sits within a few hundred µs
of the fsync floor; the vector column is the one expected to separate as rows
grow, since HNSW insert is O(log n) distance computations.)*

## Table 3 — CRUD stress: unidb (SQL) vs Postgres (relational)

Full CRUD at matched durability — not just INSERT: bulk insert, filtered and
grouped SELECT, bulk UPDATE, selected and full DELETE. Each row shows how many
records the operation touched and its throughput (records/sec). Table pre-loaded
to **100000 rows** (`MM_CRUD_ROWS` to change).

**Note on INSERT:** here each row is its **own durable commit** (one fsync/row —
the per-row latency floor, ~hundreds/sec), which is why it is far below the
batched bulk-load path in Table 3.1 (one commit per 5000 rows).

_Durability lens: unidb `fsync`, Postgres `wal_sync_method=fsync` (matched)._

Extra columns (C1, unidb only): **WAL B/row** = cumulative WAL bytes the op
appended ÷ records touched; **dec/row** = full-row heap decodes ÷ records
touched; **cols/row** = column values materialized into a Literal ÷ records
touched.

| operation | records | unidb (rec/s) | postgres (rec/s) | unidb ÷ PG | remark (winner · margin) | WAL B/row | dec/row | cols/row |
|-----------|--------:|--------------:|-----------------:|-----------:|:-------------------------|----------:|--------:|---------:|
| INSERT (per-row commit) | 100000 | 2404 | 5865 | 0.41× | **postgres** +144% | 655 | 0.00 | 0.00 |
| SELECT filtered (k<N/20, 5%) | 5000 | 2251154 | 5717278 | 0.39× | **postgres** +154% | 0 | 0.00 | 4.00 |
| SELECT grouped (GROUP BY g) | 200000 | 24874848 | 25498684 | 0.98× | **postgres** +3% | 0 | 0.00 | 1.00 |
| SELECT COUNT(*) (all) | 200000 | 260501465 | 57043699 | 4.57× | **unidb** +357% | 0 | 0.00 | 0.00 |
| UPDATE HOT-eligible (SET body, k<N/2) | 50000 | 32678 | 623769 | 0.05× | **postgres** +1809% | 410 | 1.00 | 8.00 |
| UPDATE non-HOT (SET k, k>=N AND k<3N/2) | 50000 | 20921 | 969016 | 0.02× | **postgres** +4532% | 556 | 1.00 | 8.00 |
| DELETE selected (k>=N) | 100000 | 342788 | 2504775 | 0.14× | **postgres** +631% | 39 | 0.00 | 2.00 |
| DELETE all | 100000 | 25611749 | 5001844 | 5.12× | **unidb** +412% | 0 | 0.00 | 0.00 |

### Table 3 — Known honest ceilings (verified; do not re-investigate without new evidence)

| operation | current ratio | ceiling | root cause | revisit when |
|---|---|---|---|---|
| SELECT filtered | 0.39× at 5% selectivity | ~0.40× | B-tree index path dominates at 5% selectivity (A3 gate correctly routes after ANALYZE). Remaining gap: PG parallel index scan. | Parallel B-tree candidate resolution. |
| UPDATE HOT-eligible | 0.05× | ~0.40–0.55× | Cross-page HOT (item 71) shipped; per-row fsync bottleneck remains. | Batch mini-txn (N rows per fsync). |
| UPDATE non-HOT | **0.02× (first measurement)** | ~0.07× structural | SET k (indexed): B-tree patch + per-row fsync floor. No HOT path possible. | Batch mini-txn. |
| INSERT per-row | 0.41× | ~0.55–0.60× | Per-row fsync floor. WAL B/row at 655 (Step 4 logical B-tree WAL delivered main gain). | Batch-commit or group-commit mode. |
| DELETE selected | 0.14× | ~0.14× | After WAL_XMAX_BATCH (item 44), bottleneck is page-write phase. Parallel scan ineffective at 50% selectivity. | New WAL record type reducing page-write overhead. |

## Table 3.1 — Bulk stress: insert + full-scan select at scale

| rows | unidb insert (rec/s) | postgres insert (rec/s) | insert winner · margin | unidb scan (rec/s) | postgres scan (rec/s) | scan winner · margin |
|-----:|---------------------:|------------------------:|:-----------------------|-------------------:|----------------------:|:---------------------|
| 10000 | 29608 | 27294 | **unidb** +8% | 11249644 | 8267312 | **unidb** +36% |
| 1000000 | 18760 | 27878 | **postgres** +49% | 17728754 | 52422463 | **postgres** +196% |
| 2000000 | 17299 | 27919 | **postgres** +61% | 17400080 | 50029392 | **postgres** +188% |

## Table 4 — unidb multi-model (1 atomic txn) vs the replaced stack, at scale

unidb commits **four model-writes in one atomic transaction** (relational +
`VECTOR(128)` + graph edge + event); the `PG relational only` column does the
**relational INSERT only** (1 model-write) — the stack's single-model floor.

_Durability lens: unidb `fsync`, Postgres `wal_sync_method=fsync` (matched);
single writer, so no group-commit coalescing on either side._

| txns | model-writes/txn | unidb txns/s | unidb ms/txn | PG relational only txns/s | postgres ms/txn | unidb ÷ PG-floor |
|-----:|:----------------:|-------------:|-------------:|--------------------------:|----------------:|-----------------:|
| 1000 | 4 : 1 | 167 | 5.979 | 6474 | 0.154 | 0.03× |
| 10000 | 4 : 1 | 148 | 6.751 | 7713 | 0.130 | 0.02× |
| 100000 | 4 : 1 | 42 | 23.741 | 6937 | 0.144 | 0.01× |

_(1M row entry killed — ~6h ETA at 42 txns/s; `MM_TX_SWEEP` will be capped at 100k.)_

---

## Items shipped in this run (commit cd94d71)

| item | what shipped | effect |
|------|-------------|--------|
| SQL arithmetic (`+`, `-`, `*`, `/`, `%` in SET) | `ArithOp` + `Expr::Arith` in logical/parser/executor | Unblocked `UPDATE non-HOT` bench row — **first ever measurement: 0.02×** |
| Item 72 — HNSW L0 in-memory query cache | `HnswL0Cache` per Engine, snapshot-then-merge, 256 MiB cap | Warm NEAR query cache; INSERT path unchanged (cache only on NEAR) |

---

## W4/W0 regression note

W4/W0 = 181.53× at 100k rows vs 0.73× in `report_20260717_131853.md`. The
regression is **HNSW insert cost dominating**, not fsync: W2 (HNSW) adds
18.87ms at 100k (Table 2), eating the shared-fsync advantage. The disk-based
HNSW (item 63) is the root cause — it replaced the in-memory IVF-Flat which
was faster to insert into. The item 72 L0 cache addresses NEAR query latency
but not INSERT cost. A write-optimised HNSW (buffer + batch neighbour updates)
is the next architectural lever for W4/W0.
